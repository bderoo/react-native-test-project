import { StyleSheet } from 'react-native'

import { Colors, Metrics } from '@/theme'

const styles = StyleSheet.create({
  wrapper: {
    backgroundColor: Colors.error,
    padding: Metrics.xxxLarge,
  },
})

export default styles
